# ------------------------------------------------------------------------------------------
# PRODUCTION DEPLOYMENT ENVIRONMENT VARIABLES
# Copy this file to '.env' or add these to your Render/Heroku environment config
# ------------------------------------------------------------------------------------------

# Set to 'production' to serve the static React frontend from Node
NODE_ENV=production

# The port the unified backend/frontend server will run on
PORT=5000

# Your MongoDB Atlas Connection String
# e.g., mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/warranty-tracker?retryWrites=true&w=majority
MONGO_URI=mongodb://localhost:27017/warranty-tracker

# Secret key used for signing JWT Authentication Tokens
JWT_SECRET=super_secret_jwt_key_please_change_this_in_production

# (Optional) SMTP Credentials for Automated Email Delivery
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
